plugin: netbox.netbox.nb_inventory
group_prefix: nbx_
validate_certs: True
config_context: False
group_by:
  - device_roles
  - sites
  - tenants
  - device_types
  - is_virtual
  - platforms
compose:
 ansible_network_os: platform.slug
